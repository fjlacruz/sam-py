version: 0.0
Resources:
- myLambdaFunction:
    Type: AWS::Lambda::Function
    Properties:
      Name: cdk-lambda-py # Nombre exacto de tu función Lambda
      Alias: dev # Alias que CodeDeploy gestionará
      CurrentVersion: 26 # Obtiene la versión actual de la función
