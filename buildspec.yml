version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
    - ls
    - pip install -r requirements.txt
  build:
    commands:
    - zip -r function.zip .
artifacts:
  files:
  - function.zip
  discard-paths: yes
